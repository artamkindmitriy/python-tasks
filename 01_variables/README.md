## Тема: Переменные

### Задача 1. Моя первая переменная

**Дано:** Создайте переменную, присвойте ей текст «Моя первая переменная!» и выведите значение переменной на экран. Для этого:

1. Назовите переменную, используйте только буквы латинского алфавита. Пример имени: first.
2. После этого сделайте присваивание (знак = ).
3. Вставьте нужный текст.
4. Вставьте команду вывода переменной на экран в следующей строке программы. Кавычки здесь не нужны.
5. Запустите программу и убедитесь, что в консоль выводится сообщение: «Моя первая переменная!»

**Решение:**

```python
first = "Моя первая переменная!"
print(first)
```

### Задача 2. Ошибки в коде

**Дано:** Дана программа:

```python
my color@ = 'Зелёный'
*sport& = 'Бег'
print('Меня зовут')
print(name)
name = 'Иван'
print('Мой любимый цвет:')
print(my_color)
print('Мой любимый вид спорта:')
print(спорт)
```

1. Скопируйте программу в редактор и исправьте ошибки в названиях и выводе переменных. При необходимости переставляйте строчки местами.
2. Проверьте правильность работы программы.

**Решение:**

```python
my_color = 'Зелёный'
sport = 'Бег'
name = 'Иван'
print('Меня зовут')
print(name)
print('Мой любимый цвет:')
print(my_color)
print('Мой любимый вид спорта:')
print(sport)
```

### Задача 3. **Приветствие пользователя**

**Дано:** Повторите действия из урока и создайте программу, которая запрашивает у пользователя имя и затем выводит для него приветствие. Для этого:

- Создайте переменную и присвойте ей команду input(). Внутри скобок команды напишите приглашение, например: «Введите имя».
- В следующих двух строках напишите вывод приветствия и переменной на экран.
- Запустите программу, введите в консоль имя и нажмите Enter.
- Убедитесь, что программа работает верно.

**Решение:**

```python
name = input("Введите имя: ")

print(f"Привет {name}!")
```

### Задача 4. Неверная программа

**Дано:** Ниже приведена программа, которая должна запрашивать фамилию пользователя и выводить её на экран. Но её написали неправильно. Скопируйте программу в редактор и исправьте все ошибки. Убедитесь, что программа работает.

```python
sur name = input('Введите фамилию: ')
print('Ваша фамилия - ')
print('sur name')
```

**Решение:**

```python
surname = input('Введите фамилию: ')
print(f'Ваша фамилия - {surname}')
```

### Задача 5. Склейка

**Дано:** Напишите программу, которая запрашивает на вход два слова, записывает их в разные переменные, затем склеивает их и выводит на экран полученную строку.

*Пример:*

```
Введите первое слово: Всем

Введите второе слово: привет

Всем привет
```

**Решение:**

```python
first_word = input("Введите первое слово: ")
second_word = input("Введите второе слово: ")
print(first_word + " " + second_word)
```

### Задача 6. **Найти пропавшую переменную**

**Дано:** Найдите в программе необъявленную переменную и объявите её, присвоив ей значение ‘Кот’.

```python
client = 'Петя'
print(client)
print(‘ и ’)
print(pet)
```

**Решение:**

```python
client = 'Петя'
pet = "Кот"
print(client)
print("и")
print(pet)
```

### Задача 7. **Вход в систему**

**Дано:** Исправьте программу и допишите необходимые команды для получения нужного результата. Будьте внимательны при исправлении и помните о правилах названия переменных.

```python
first name = input('Введите имя пользователя: ')

grииting = 'Утро доброе'

print(greeting, first name)

intro = "К сожалению, у Вас нет доступа к системе."

info == "Пожалуйста, обратитесь к системному администратору."
```

*Пример:*

```
Введите имя пользователя: Роман
Привет, Роман
К сожалению, у Вас нет доступа к системе.
Пожалуйста, обратитесь к системному администратору.
```

**Решение:**

```python
first_name = input('Введите имя пользователя: ')
print(f"Привет, {first_name}")
print("К сожалению, у Вас нет доступа к системе.")
print("Пожалуйста, обратитесь к системному администратору.")
```

### Задача 8. Полёт

**Дано:** Напишите программу для сервиса заказа билетов, которая запрашивает у пользователя город вылета и город прилёта. Затем выведите их в одну строку через тире. Обратите внимание на свои переменные: названия должны отражать содержимое.

**Решение:**

```python
from_to = input("Город вылета: ")
to_city = input("Город прилёта: ")

print(f"{from_to} - {to_city}")
```

### Задача 9. **Обмен значений двух переменных**

**Дано:** Дана программа, которая запрашивает у пользователя два слова, а затем выводит их на экран два раза. Скопируйте эту программу в редактор и проверьте.

```python
a = input('Введите первое слово: ')
b = input('Введите второе слово: ')
print(a, b)
# Здесь вы можете написать любое количество строк кода 
# Менять, удалять переставлять строки выше и ниже нельзя
print(a, b)
```

Вам нужно поменять значения переменных a и b местами. Не меняйте уже написанный код (input и print). Можете вставить между принтами любое количество строк кода, которое нужно для решения.

*Пример:*

```
Введите первое слово: вода
Введите второе слово: сок
вода сок
сок вода
```

**Решение:**

```python
a = input('Введите первое слово: ')
b = input('Введите второе слово: ')
print(a, b)
temp = a
a = b
b = temp
print(a, b)
```